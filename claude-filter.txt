# What travels between machines. rclone filter rules, first match wins.
# Paths are relative to ~/.claude. Edit to taste.

# --- Heavy / secret: never sync (listed first so they win) ---
# Whole plugins/ dir is excluded: its manifests carry absolute OS-specific install
# paths that break on another machine. Plugins are restored from plugins-manifest.json.
- /plugins/**
- /projects/**
- /sessions/**
- /history.jsonl
- /shell-snapshots/**
- /file-history/**
- /cache/**
- /image-cache/**
- /paste-cache/**
- /downloads/**
- /ide/**
- /backups/**
- /tasks/**.lock
- /.credentials.json
- /settings.local.json
- /*.log

# --- Synced (ON) ---
+ /settings.json
+ /MEMORY.md
+ /mcp-servers.json
+ /skills/**
+ /agents/**
+ /commands/**
+ /memory/**
+ /plans/**
+ /tasks/**
+ /plugins-manifest.json

# --- Everything else excluded ---
- *
