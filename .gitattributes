# Shell scripts MUST stay LF or they break on macOS/Linux (\r in shebang).
*.sh text eol=lf
# PowerShell is fine either way; keep CRLF on Windows checkouts.
*.ps1 text eol=crlf
# Everything else: normalize to LF in the repo.
* text=auto eol=lf
